<%= form_for(@play_style) do |f| %>
  <% if @play_style.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@play_style.errors.count, "error") %> prohibited this play_style from being saved:</h2>

      <ul>
      <% @play_style.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br />
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label :description %><br />
    <%= f.text_area :description %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
